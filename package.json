{
  "name": "config",
  "version": "1.0.0",
  "description": "Save ENV vars in file and turn into config object",
  "main": "config.js",
  "scripts": {
    "test": "make test"
  },
  "pre-commit": [
    "test"
  ],
  "keywords": [
    "config",
    "12factor",
    "env"
  ],
  "author": "Mark Garner",
  "license": "ISC",
  "dependencies": {
    "node-env-configuration": "0.0.5",
    "node-env-file": "^0.1.7"
  },
  "devDependencies": {
    "make-lint": "^1.0.1",
    "mocha": "^2.2.5",
    "pre-commit": "^1.0.10"
  }
}
