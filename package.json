{
  "name": "env-config",
  "version": "1.0.0",
  "description": "Get config objects from ENV variables defined in config.env",
  "main": "env-config.js",
  "scripts": {
    "test": "make test",
    "coverage": "istanbul cover node_modules/.bin/_mocha -- test/*.js -R spec"
  },
  "pre-commit": [
    "test"
  ],
  "keywords": [
    "config",
    "12factor",
    "env"
  ],
  "author": "Mark Garner",
  "license": "ISC",
  "dependencies": {
    "node-env-configuration": "0.0.5",
    "node-env-file": "^0.1.7"
  },
  "devDependencies": {
    "make-lint": "^1.0.1",
    "mocha": "^2.2.5",
    "pre-commit": "^1.0.10"
  }
}
